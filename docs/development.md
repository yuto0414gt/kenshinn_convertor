# kenshinn_convertor — 開発ロードマップ / 経緯

このファイルは開発の経緯・今後の方針・未着手課題をまとめる。仕様そのものは [`spec.md`](spec.md)、用語は [`../CONTEXT.md`](../CONTEXT.md)。

履歴は「1 行 + → 該当箇所リンク」を基本に、詳細は各ファイル / Issue へ逃がす。

---

## Phase ロードマップ（企業プランで縦に通す → 横展開）

### Phase 0 — 整備・要件確定 ← いま here

- [x] ディレクトリ整備（`docs/` `src/` `samples/` `outputs/`）
- [x] README / CONTEXT / spec / development の初版
- [x] `.gitignore`（患者情報・画像・PDF・Excel を除外）
- [x] 企業プランの入出力サンプル受領 → spec を実物ベースに更新
- [x] spec Q2 確定: 判定＋アドバイス自動／総合所見手入力。Q3 確定: 例外は空欄＋色付け
- [x] テンプレ Excel 作成（Q4）: `templates/企業健診プラン.xlsx`（テンプレ / 記入例）
- [x] `/init` で `CLAUDE.md` を起草
- [ ] 残: 検査結果票の日付列ルール（Q1）／入力ファイルの渡し方（Q6）

### Phase 1 — 縦の素通し（企業・1 患者・実測値転記）

**書き込み側（OCR 不要）は完成・テスト済み。OCR 側は認証＋実画像が揃ったら着手。**

- [x] テンプレ Excel の対応セルへ**実測値**を書き込む（`src/kenshin/excel_writer.py`、対応づけは `configs/企業.json`）
- [x] BMI・年齢の算出（`src/kenshin/compute.py`、和暦パース込み）
- [x] 例外（確信度低・要確認）は空欄＋黄色塗り
- [x] `run.py demo` / `from-json` で入力→個人票が通ることを確認（`tests/test_basic.py`）
- [x] 画像→値の抽出を Claude(LLM)で実装（`extract_llm.py`、健診特化プロンプト＋構造化出力＋ _uncertain→色付け）
- [ ] `ANTHROPIC_API_KEY` を設定し、実サンプル3枚で抽出精度を検証（手書き欄の取りこぼし・誤読を確認）
- [ ] ID 突合の明示化（現状は LLM が両帳票をまとめて読む。1患者=1組前提）

### Phase 2 — 健診用語辞書・例外処理

- [ ] 健診用語辞書による手書き誤読補正（「なし」「動悸」等）
- [ ] 数値の妥当性チェック（桁・単位・範囲）
- [ ] 確信度しきい値と例外の仕分け（Q3 の方式で）

### Phase 3 — 判定・アドバイスの自動化（確定スコープ）

- [ ] 基準範囲・判定ロジックを設定ファイル化（種別ごと）
- [ ] 実測値×基準範囲から判定 A〜E を算出
- [ ] 判定→生活アドバイスの対応表で定型文を自動挿入
- [ ] 総合所見は空欄のまま（医師手入力）

### Phase 4 — 種別・運用の横展開

- [ ] ライト・特定プラン対応（読み取り範囲・対応づけを設定追加で）
- [ ] バッチ処理（フォルダ一括変換）

---

## 未着手・検討中の課題

- **検査結果票の日付列（spec Q1）**: 受診日列を採用・無い項目は未実施、が暫定。要確認。
- **判定ロジック/基準範囲の定義（spec Q2 派生）**: 種別ごとの A〜E の境界・アドバイス文の対応表を確定する（基準範囲は `templates/企業健診プラン.xlsx` に記載済み）。
- **ライト・特定のサンプル（spec Q5）**: 企業を通してから。

---

## 開発経緯ログ

- **2026-06-29** プロジェクト発足。ディレクトリ・初版ドキュメント作成。同日、企業プランの入出力サンプル（手書きチェック表＋印字検査結果票＋出力個人票）を受領し、spec / CONTEXT / README を実物ベースに更新。OCR=Google Vision 確定、移植配慮なし＝ローカル最短方針を確認。
- **2026-06-29** Phase 1 着手。`templates/企業健診プラン.xlsx`（テンプレ/記入例）を写真から作成。書き込み側（`excel_writer`/`compute`/`pipeline`/`run.py`）を実装・テスト（`tests/test_basic.py`）。OCR 側（`ocr_vision`/`preprocess`/`extract`）は雛形。
- **2026-06-29** Phase 1 検証(A)。実スキャン3枚の値を `from-json` で投入 → 生成個人票が期待出力(IMG_8306)と **全35項目一致**。BMI/年齢/未実施(Q1列選択)が正しく動作。手書きの自覚症状「なし」は、期待出力の OCR 誤読「エレ」に対しこちらは正しく「なし」を出力。
- **2026-06-29** OCR エンジンを **Google Vision → Claude(LLM) 画像認識へ変更**（`extract_llm.py`、`run.py llm`）。手書き×健診特化を認識の最中に効かせるのが狙い。Vision 用の `ocr_vision.py`/`preprocess.py`/`extract.py` は削除。出力は `from-json` と同じ値 dict なので後段は不変。次は `ANTHROPIC_API_KEY` を設定して実データで精度検証。
