# kenshinn_convertor

健診の問診票（手書き）をスキャンし、OCR で読み取って Excel の適切なセルへ書き出すツール。本ファイルはこのプロジェクトで使う言葉の定義（用語集）であり、実装の詳細は書かない。サンプルを見て確定したら随時更新する。

## Language

**チェック表 (問診票 / questionnaire)**:
健診で記入する **手書き**の用紙（正式名「健康診断チェック表」）。身体計測・血圧・視力・聴力・既往歴・自覚症状・画像検査の所見などを含む。本ツールの入力①。1 枚 = 1 人。
_Avoid_: シート, フォーム, 用紙（一般語と区別する）

**検査結果票 (lab result sheet)**:
採血・尿検査の結果が**印字**された帳票（2 ページ）。本ツールの入力②。基準値列のほか複数の日付列を持つ。
_Avoid_: 結果表, データシート

**個人票 (output / 個人票)**:
入力①②を統合して書き出す Excel（正式名「健康診断個人票」）。本ツールの出力。検査ブロックごとに `実測値 / 判定 / 基準範囲 / 生活アドバイス` の 4 列を持つ。
_Avoid_: 結果票（入力②の検査結果票と必ず区別する）

**健診種別 (exam type)**:
健診の種類。**企業 / ライト / 特定** の 3 種があり、検査項目（読み取り範囲・出力項目）が異なる。処理開始時に選択する。現状は企業のみ実装。
_Avoid_: コース, プラン（呼び分けが必要なとき以外は「健診種別」）

**ID 突合 (patient match)**:
入力①のIDと入力②の患者番号（例 `000000000`）が一致することで、同一患者の 2 帳票を結びつけること。
_Avoid_: 紐付け, マッチング

**判定 (assessment / A〜E)**:
各検査項目の評価区分。`A:異常なし / B:軽度異常 / C:要再検査・生活改善 / D:要精密検査・治療 / E:治療中`。実測値と基準範囲から決まる（自動化範囲は spec Q2）。
_Avoid_: 評価, ランク

**生活アドバイス (advice)**:
判定に応じて個人票へ載せる定型の指導文。
_Avoid_: コメント, 所見（医師の総合所見と区別する）

**設問 (field)**:
問診票上の 1 つの質問項目（例: 「自覚症状」「既往歴」「服薬」「喫煙」）。OCR で読み取り、Excel の特定セルへ対応づける単位。
_Avoid_: 項目, 質問, カラム（Excel 側のセルと混同しない）

**回答 (answer / value)**:
1 つの設問に対して患者が記入した内容。記入形式は **手書き自由記述**・**チェック / 丸囲み**・**数値**などがありうる（サンプルで確定）。
_Avoid_: 値, 入力（曖昧にしない）

**自覚症状 (subjective symptom)**:
患者が自分で感じている症状の自由記述。本ツールが最も重視する手書き読み取り対象。健診問診の用語を優先して認識する。
_Avoid_: 症状, 主訴

**健診用語辞書 (term dictionary)**:
OCR の認識・補正に使う、健診問診で頻出する語のリスト（例: 「動悸」「息切れ」「胸痛」「めまい」「服薬中」）。誤読をこの辞書で正しい語へ寄せる。
_Avoid_: 辞書（複数あるとき以外は「健診用語辞書」と呼ぶ）, 単語帳

**テンプレ Excel (output template)**:
回答を書き込む先の、セル配置が決まった Excel の雛形。「どの設問をどのセルに入れるか」の対応づけの土台。
_Avoid_: 出力先, フォーマット（出力ファイルそのものと区別する）

**セル対応づけ (cell mapping)**:
設問 → テンプレ Excel のセル（例: `自覚症状 → C5`）の対応表。本ツールの配置ロジックの中核。
_Avoid_: マッピング（日本語では「セル対応づけ」で統一）, 紐付け

**確信度 (confidence)**:
OCR が読み取り結果にどれだけ自信があるかの度合い。低いものは例外として仕分ける判断基準になる。
_Avoid_: スコア, 精度

**例外 (exception / flagged)**:
自動で確定できなかった回答。読み取れない・確信度が低い・想定外の記入など。Excel にそのまま入れず、別途レビューできる形にする（処理方法はサンプルを見て一緒に決める）。
_Avoid_: エラー, 不明（どう仕分けるかは確定後に定義）

**前処理 (preprocessing)**:
OCR 前に画像を整える工程。PDF→画像変換・傾き補正・二値化・設問領域の切り出しなど。
_Avoid_: 加工

## Flagged ambiguities

- **「項目」**: 問診票側の質問なのか Excel 側の列なのか曖昧。問診票側は **設問**、Excel 側は **セル** と必ず呼び分ける。
- **「フォーマット」**: 入力の問診票レイアウトなのか出力 Excel なのか曖昧。出力雛形は **テンプレ Excel** と呼ぶ。
- **チェック表は定型**（確定）。設問位置が固定なので、向き補正後に領域切り出し（ゾーン OCR）で精度を上げられる。ただし**スキャン時の向き違い**には備える。
- **「結果票」と「個人票」の混同に注意**: 入力②は **検査結果票**、出力は **個人票**。必ず呼び分ける。

## 例：開発者とドメイン専門家の会話

> **開発者**: この問診票、設問の位置はいつも同じ場所ですか？
> **Kajita**: 基本は同じ定型の **問診票**。だから **設問** ごとに場所が決まってて、**自覚症状** はここ、既往はここ、って **セル対応づけ** ができるはず。
> **開発者**: 手書きが読めなかったらどうします？
> **Kajita**: それは **例外**。無理に **テンプレ Excel** のセルへ入れず、後で見直せる形にしたい。**確信度** が低いものも同じ扱いで。
> **開発者**: 健診ならではの言葉は OCR が苦手そうですが。
> **Kajita**: そこは **健診用語辞書** で寄せてほしい。「動悸」「息切れ」みたいな頻出語を優先で。
