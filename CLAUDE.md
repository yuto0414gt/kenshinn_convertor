# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

統括 [`../CLAUDE.md`](../CLAUDE.md)（AppDevelopment 共通ルール）を継承する。本ファイルはこのアプリ固有の事項のみを書く。

## このアプリは何か

健診の **2 帳票**（手書きの健診チェック表＋印字の検査結果票）をスキャンから OCR で読み取り、両者を統合して **健康診断個人票（Excel）** を生成するツール。Python / macOS ローカル。

- 仕様の現物・セル対応づけ・確定事項 → [`docs/spec.md`](docs/spec.md)（**実装前に必読**）
- このプロジェクトの用語定義 → [`CONTEXT.md`](CONTEXT.md)（「検査結果票」＝入力②、「個人票」＝出力、を混同しない）
- ロードマップ・経緯 → [`docs/development.md`](docs/development.md)
- README は仕様の入口

## 現在地

**Phase 1 進行中。書き込み側（OCR 不要部分）は完成・テスト済み。** OCR 抽出（画像→値）は Vision 認証＋実スキャン画像が揃ってから着手。

```bash
pip install -r requirements.txt    # 書き込みは openpyxl のみ。抽出には anthropic も要る
python run.py demo                  # サンプル値→ outputs/企業_個人票.xlsx（API不要・動作確認）
python run.py from-json 値.json     # 抽出済みの値から個人票を生成（API不要）
python run.py llm 表.jpg 採血.jpg   # 画像→ Claude抽出→個人票（要 ANTHROPIC_API_KEY・課金）
python tests/test_basic.py          # 最小テスト（pytest 不要・API不要）
```

主要モジュール（`src/kenshin/`）: `excel_writer`（テンプレへ書き込み＋例外色付け）/ `compute`（BMI・年齢・和暦）/ `pipeline`（統合）/ `extract_llm`（Claude で画像→値・要 API キー）/ `models`。対応づけは `configs/<種別>.json`。
LLM 抽出は `run.py from-json` と**同じ値 dict** を出力するので、後段（算出・色付け・書き込み）は共通。プロンプト調整は `extract_llm.py` の `SYSTEM` を編集。

## 確定済みの設計判断（推測で覆さない）

これらはユーザーと合意済み。覆す提案をする前に必ず確認すること。

- **OCR/抽出 = Claude（LLM）の画像認識**（`claude-opus-4-8`、`src/kenshin/extract_llm.py`）。当初 Google Vision を想定 → 手書き×健診特化の精度と項目抽出の素直さを優先して 2026-06-29 に LLM へ切替。要 `ANTHROPIC_API_KEY`。**患者画像を API へ送る**点は院内規程と整合を取る前提。
- **移植（Windows / GAS）への配慮は当面しない。macOS ローカル完成を最短で目指す。** 過剰抽象化しない。
- **入力①は定型レイアウト**。前処理で**向き補正**（4 方向＋微小傾き）→ 定型なので**領域切り出し（ゾーン OCR）**で精度を上げる。
- **入力①②は ID / 患者番号で突合**（例 `000000000`）。
- **健診種別（企業 / ライト / 特定）で検査項目が異なる**。処理開始時に種別を選択し、種別ごとに読み取り範囲・出力テンプレ・基準範囲を切り替える。**現状は企業のみ**。
- **判定 A〜E と生活アドバイスは自動算出／挿入。医師の総合所見は空欄（手入力）。**
- **OCR が読めない・確信度が低い項目は、個人票の該当セルを空欄にして色付け**（医師がそこだけ手で埋める。値を勝手に入れない）。

## アーキテクチャ方針（実装時の指針）

- **基準範囲・判定境界（A〜E）・判定→アドバイス文の対応表・読み取り範囲・セル対応づけは、コードに直書きせず設定ファイルへ外出しする。** 種別（企業/ライト/特定）を「設定を足すだけ」で増やせる形にするのが目的。検査項目がコードに散ると種別追加のたびに改修が要る。
- **出力は既存テンプレ Excel へ書き込む方式**（体裁・基準範囲・凡例を壊さない）。`openpyxl` 想定。テンプレは **`templates/企業健診プラン.xlsx`**（写真から新規作成済み・追跡対象）。`テンプレ` シート＝書き込み先（データ欄は空）、`記入例` シート＝期待出力リファレンス。種別ごとに `templates/<種別>.xlsx` を追加。書き込み先セルは `docs/spec.md §3` 参照。
- 想定パイプライン: 前処理（PDF→画像・向き補正・領域切り出し）→ Vision OCR → 項目抽出 → ID 突合 → 算出（BMI・年齢・判定・アドバイス）→ テンプレへ書き込み → 例外セルの色付け。
- 手書き数値は**字種・桁・単位で妥当性チェック**（視力 0.0〜2.0、血圧 2〜3 桁 等）。誤読は**健診用語辞書**で頻出語（「なし」「動悸」等）へ補正。出力サンプルでも自覚症状「なし」が「エレ」と誤読されており、手書き誤読は必ず起きる前提。

## Deny zone（触らない）

統括ルールのプライバシー保護に加え、本アプリ固有:

- **`samples/`・`outputs/`** … 実患者の自覚症状・検査値を含む。`.gitignore` 済み。中身を読む必要があっても**新規に実名・実データをコードへ書き込まない**。
- **認証キー** … Google Vision の `*-credentials.json` 等。`.gitignore` 済み。コミットしない。検査結果票を Vision（クラウド）へ送る点は院内規程との整合をユーザー判断に委ねる。
- 画像 / PDF / Excel 拡張子は `.gitignore` で一括除外。テンプレ等を意図的に追跡する場合のみ `git add -f`。

## ドキュメント更新の約束

- 仕様判断を決めたら **`docs/spec.md` §7「決まったことログ」に日付つきで追記**。未確定は §5 に集約。
- README の **`## Current Status`** は統括 `/status` が読むので、進捗が動いたら更新する。
- 用語を増やす / 変えるときは `CONTEXT.md` を更新（呼び分けの一貫性を保つ）。

## 未確定（実装前に詰める）

- 検査結果票の日付列の選び方（暫定: 受診日に一致する列を採用・無い項目は未実施。同日複数列の優先順位は未確定）— spec Q1
- 入力ファイルの渡し方（複数ページ PDF か個別画像か）— spec Q6
- ライト・特定プランのサンプル — spec Q5
